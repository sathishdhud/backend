package com.hotelworks.scheduler;

import com.hotelworks.HotelManagementApplication;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = HotelManagementApplication.class)
public class AuditDateSchedulerTest {

    @Test
    public void contextLoads() {
        // This test verifies that the application context loads successfully
        // and all scheduled tasks are properly configured
    }
}